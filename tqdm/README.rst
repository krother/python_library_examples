
How to create a progress bar on the console?
============================================

The `tqdm` library has just one purpose: Drawing progress bars.

Install it with:

.. code::
    
    pip install tqdm

Here is an example code piece:

.. literalinclude:: progress_bar.py
