
How to optimize parameters of a function with SciPy?
====================================================

.. figure:: fit.png

Scipy is a Python library for **numerical analyses** like fitting functions, signal processing, Fourier Transform, generating random datasets and many more. Scipy uses `numpy` and `matplotlib`.

Install it with:

.. code::

    pip install scipy

The following code creates noisy data and fits a polynomial function to it:

.. literalinclude:: curve_fit.py

In :download:`peak_recognition.ipynb` you find a notebook for recognizing peaks.

.. seealso::

   `scipy.org/ <http://scipy.org/>`__
